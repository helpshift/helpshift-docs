---
sidebar_position: 140
title: Impact Analysis
description: "The following numbers are representative of a skeletal sample appintegrated with the Helpshift Android SDK."
---

import {
  Admonition,
  CodeBlock,
  Tabs,
  TabItem,
  LatestSdkVersion,
  Centered,
  Image,
  Intro,
  SideBySide,
  DownloadButton,
  Steps,
  Step,
} from "@site/src/components/forDocs";

<Admonition type="danger" title="Important">

Helpshift’s Legacy SDKs (SDK Version <=7.x.x) will see end of life as of 31 Dec 2022 and end of support as of 31 March 2023.

</Admonition>

# Impact Analysis {#performance}

<Intro>

The following numbers are representative of a skeletal sample app
integrated with the Helpshift Android SDK.

</Intro>

## v7.11.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.92 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1. The 1.92 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view). If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will add 1.46 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 9614 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `9614` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.39` |
| showFAQs                                     |                    `3.13` |
| showFAQSection                               |                    `2.90` |
| showSingleFAQ                                |                    `4.45` |
| showConversation (with no open Conversation) |                    `3.54` |
| showConversation (with an open Conversation) |                    `5.12` |
| showConversation (conversational mode)       |                    `2.93` |

<Admonition type="info" title="Note">

Tested with a demo app on Pixel4A running Android 12. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.9.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.87 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1. The 1.87 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view). If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will add 1.39 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8214 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `8214` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.39` |
| showFAQs                                     |                    `3.13` |
| showFAQSection                               |                    `2.90` |
| showSingleFAQ                                |                    `4.45` |
| showConversation (with no open Conversation) |                    `3.40` |
| showConversation (with an open Conversation) |                    `4.25` |
| showConversation (conversational mode)       |                    `2.93` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 7 running Android 10. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.9.0-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.07 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1. The 2.07 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view). If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will add 1.59 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 9335 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `9335` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.35` |
| showFAQs                                     |                    `3.10` |
| showFAQSection                               |                    `3.00` |
| showSingleFAQ                                |                    `4.40` |
| showConversation (with no open Conversation) |                    `3.95` |
| showConversation (with an open Conversation) |                    `4.60` |
| showConversation (conversational mode)       |                    `3.00` |
| showInbox (Campaigns)                        |                    `1.30` |
| showMessage (Campaigns)                      |                    `3.13` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 7 running Android 10. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.8.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.88 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.88 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view, and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.40 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8330 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `8330` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.40` |
| showFAQs                                     |                    `3.23` |
| showFAQSection                               |                    `2.73` |
| showSingleFAQ                                |                    `4.15` |
| showConversation (with no open Conversation) |                    `3.55` |
| showConversation (with an open Conversation) |                    `4.35` |
| showConversation (conversational mode)       |                    `2.85` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 7 running Android 10. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.8.0-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.06 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 2.06 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view, and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.58 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 9271 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `9271` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.30` |
| showFAQs                                     |                    `3.23` |
| showFAQSection                               |                    `2.70` |
| showSingleFAQ                                |                    `4.00` |
| showConversation (with no open Conversation) |                    `3.90` |
| showConversation (with an open Conversation) |                    `4.54` |
| showConversation (conversational mode)       |                    `3.00` |
| showInbox (Campaigns)                        |                    `1.20` |
| showMessage (Campaigns)                      |                    `3.29` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 7 running Android 10. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.2

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.83 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.83 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.35 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7956 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `7956` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `2.88` |
| showFAQSection                               |                    `2.73` |
| showSingleFAQ                                |                    `3.82` |
| showConversation (with no open Conversation) |                    `2.92` |
| showConversation (with an open Conversation) |                    `1.85` |
| showConversation (conversational mode)       |                    `2.80` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.2-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.02 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 2.02 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.54 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8919 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `8919` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.24` |
| showFAQs                                     |                    `2.90` |
| showFAQSection                               |                    `2.79` |
| showSingleFAQ                                |                    `3.86` |
| showConversation (with no open Conversation) |                    `2.96` |
| showConversation (with an open Conversation) |                    `1.89` |
| showConversation (conversational mode)       |                    `2.80` |
| showInbox (Campaigns)                        |                    `1.30` |
| showMessage (Campaigns)                      |                    `3.39` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.1

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.83 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.83 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.35 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7956 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `7956` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `2.88` |
| showFAQSection                               |                    `2.73` |
| showSingleFAQ                                |                    `3.82` |
| showConversation (with no open Conversation) |                    `2.92` |
| showConversation (with an open Conversation) |                    `1.85` |
| showConversation (conversational mode)       |                    `2.80` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.1-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.02 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 2.02 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.54 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8919 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `8919` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.24` |
| showFAQs                                     |                    `2.90` |
| showFAQSection                               |                    `2.79` |
| showSingleFAQ                                |                    `3.86` |
| showConversation (with no open Conversation) |                    `2.96` |
| showConversation (with an open Conversation) |                    `1.89` |
| showConversation (conversational mode)       |                    `2.80` |
| showInbox (Campaigns)                        |                    `1.30` |
| showMessage (Campaigns)                      |                    `3.39` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.83 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.83 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.35 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7956 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `7956` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `2.88` |
| showFAQSection                               |                    `2.73` |
| showSingleFAQ                                |                    `3.82` |
| showConversation (with no open Conversation) |                    `2.92` |
| showConversation (with an open Conversation) |                    `1.85` |
| showConversation (conversational mode)       |                    `2.80` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.7.0-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.02 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 2.02 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.54 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8919 methods to your dex:

| Package                                                 | Method Count |
| ------------------------------------------------------- | -----------: |
| **com.helpshift**                                       |       `8919` |
| **com.android.support:design:** (includes recyclerview) |       `6673` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.24` |
| showFAQs                                     |                    `2.90` |
| showFAQSection                               |                    `2.79` |
| showSingleFAQ                                |                    `3.86` |
| showConversation (with no open Conversation) |                    `2.96` |
| showConversation (with an open Conversation) |                    `1.89` |
| showConversation (conversational mode)       |                    `2.80` |
| showInbox (Campaigns)                        |                    `1.30` |
| showMessage (Campaigns)                      |                    `3.39` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.3

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.73 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.73 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.25 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7038 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7038` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `2.81` |
| showFAQSection                               |                    `2.54` |
| showSingleFAQ                                |                    `3.97` |
| showConversation (with no open Conversation) |                    `3.71` |
| showConversation (with an open Conversation) |                    `3.49` |
| showConversation (conversational mode)       |                    `3.43` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.3-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.91 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.91 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.43 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8001 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `8001` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.25` |
| showFAQs                                     |                    `3.10` |
| showFAQSection                               |                    `2.84` |
| showSingleFAQ                                |                    `3.81` |
| showConversation (with no open Conversation) |                    `3.64` |
| showConversation (with an open Conversation) |                    `3.78` |
| showConversation (conversational mode)       |                    `2.91` |
| showInbox (Campaigns)                        |                     `1.3` |
| showMessage (Campaigns)                      |                    `3.49` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.2

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.70 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.70 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.22 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7208 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7208` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `2.94` |
| showFAQSection                               |                    `2.70` |
| showSingleFAQ                                |                    `3.79` |
| showConversation (with no open Conversation) |                    `3.72` |
| showConversation (with an open Conversation) |                    `3.30` |
| showConversation (conversational mode)       |                    `3.21` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.2-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.88 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.88 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.40 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8167 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `8167` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `2.99` |
| showFAQSection                               |                    `2.76` |
| showSingleFAQ                                |                    `3.94` |
| showConversation (with no open Conversation) |                    `3.78` |
| showConversation (with an open Conversation) |                    `3.25` |
| showConversation (conversational mode)       |                    `2.87` |
| showInbox (Campaigns)                        |                     `1.0` |
| showMessage (Campaigns)                      |                    `3.43` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.70 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.70 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.22 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7109 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7109` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `2.94` |
| showFAQSection                               |                    `2.70` |
| showSingleFAQ                                |                    `3.79` |
| showConversation (with no open Conversation) |                    `3.72` |
| showConversation (with an open Conversation) |                    `3.30` |
| showConversation (conversational mode)       |                    `3.21` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.6.0-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.88 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.4.2 and Gradle v5.1.1.
The 1.88 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.40 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 8068 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `8068` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6673` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `2.99` |
| showFAQSection                               |                    `2.76` |
| showSingleFAQ                                |                    `3.94` |
| showConversation (with no open Conversation) |                    `3.78` |
| showConversation (with an open Conversation) |                    `3.25` |
| showConversation (conversational mode)       |                    `2.87` |
| showInbox (Campaigns)                        |                     `1.0` |
| showMessage (Campaigns)                      |                    `3.43` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.5.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.80 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 1.80 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.27 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6911 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `6911` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `2.85` |
| showFAQSection                               |                    `2.69` |
| showSingleFAQ                                |                    `3.75` |
| showConversation (with no open Conversation) |                    `3.78` |
| showConversation (with an open Conversation) |                    `3.18` |
| showConversation (conversational mode)       |                    `3.12` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.5.0-withCampaigns

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 2.00 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 2.00 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.46 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7868 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7868` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `2.90` |
| showFAQSection                               |                    `2.76` |
| showSingleFAQ                                |                    `3.80` |
| showConversation (with no open Conversation) |                    `3.82` |
| showConversation (with an open Conversation) |                    `3.14` |
| showConversation (conversational mode)       |                    `2.62` |
| showInbox (Campaigns)                        |                    `0.90` |
| showMessage (Campaigns)                      |                    `3.22` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.4.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.96 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 1.96 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.42 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7625 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7625` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `2.92` |
| showFAQSection                               |                    `2.74` |
| showSingleFAQ                                |                    `3.79` |
| showConversation (with no open Conversation) |                    `3.88` |
| showConversation (with an open Conversation) |                    `3.15` |
| showConversation (conversational mode)       |                    `2.28` |
| showInbox (Campaigns)                        |                    `0.88` |
| showMessage (Campaigns)                      |                    `3.49` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.4.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.46 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 1.46 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.93 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7612 methods to your dex:

| Package                                                                   | Method Count |
| ------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                         |       `7612` |
| **com.android.support:design:28.0.0**(includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `1.10` |
| showFAQSection                               |                    `0.95` |
| showSingleFAQ                                |                    `2.00` |
| showConversation (with no open Conversation) |                    `2.06` |
| showConversation (with an open Conversation) |                    `1.28` |
| showConversation (conversational mode)       |                    `1.37` |
| showInbox (Campaigns)                        |                    `1.15` |
| showMessage (Campaigns)                      |                    `3.57` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.3.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.95 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 1.95 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.42 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7547 methods to your dex:

| Package                                                                    | Method Count |
| -------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                          |       `7547` |
| **com.android.support:design:28.0.0** (includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `4.39` |
| showFAQSection                               |                    `2.76` |
| showSingleFAQ                                |                    `4.18` |
| showConversation (with no open Conversation) |                    `3.18` |
| showConversation (with an open Conversation) |                    `3.35` |
| showConversation (conversational mode)       |                    `2.58` |
| showInbox (Campaigns)                        |                    `0.88` |
| showMessage (Campaigns)                      |                    `3.49` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.3.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.46 MB to your application's release APK. This analysis was done with Android Gradle Plugin v3.0.0 and Gradle v4.1.
The 1.46 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.92 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7534 methods to your dex:

| Package                                                                   | Method Count |
| ------------------------------------------------------------------------- | -----------: |
| **com.helpshift**                                                         |       `7534` |
| **com.android.support:design:28.0.0**(includes recyclerview and cardview) |       `6672` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `2.58` |
| showFAQSection                               |                    `0.95` |
| showSingleFAQ                                |                    `1.63` |
| showConversation (with no open Conversation) |                    `1.36` |
| showConversation (with an open Conversation) |                    `1.68` |
| showConversation (conversational mode)       |                    `1.47` |
| showInbox (Campaigns)                        |                    `1.15` |
| showMessage (Campaigns)                      |                    `3.57` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.2.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.80 MB to your application's release APK. This analysis was done with Android Plugin v3.0.0 and Gradle v4.1.
The 1.80 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.40 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7492 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7492` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.23` |
| showFAQs                                     |                    `4.40` |
| showFAQSection                               |                    `2.77` |
| showSingleFAQ                                |                    `3.39` |
| showConversation (with no open Conversation) |                    `3.58` |
| showConversation (with an open Conversation) |                    `3.12` |
| showConversation (conversational mode)       |                    `3.36` |
| showInbox (Campaigns)                        |                    `0.56` |
| showMessage (Campaigns)                      |                    `3.08` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.2.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.31 MB to your application's release APK. This analysis was done with Android Plugin v3.0.0 and Gradle v4.1.
The 1.31 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.91 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7479 methods to your dex:

| Package                                                      | Method Count |
| ------------------------------------------------------------ | -----------: |
| **com.helpshift**                                            |       `7479` |
| **com.android.support:design:26.0.2**(includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                   |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.22` |
| showFAQs                                     |                    `2.61` |
| showFAQSection                               |                    `0.98` |
| showSingleFAQ                                |                    `1.83` |
| showConversation (with no open Conversation) |                    `1.77` |
| showConversation (with an open Conversation) |                    `1.63` |
| showConversation (conversational mode)       |                    `1.56` |
| showInbox (Campaigns)                        |                    `0.83` |
| showMessage (Campaigns)                      |                    `3.15` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.1.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.75 MB to your application's release APK. This analysis was done with Android Plugin v3.1.1 and Gradle v4.4.
The 1.75 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.35 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7103 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7103` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `3.91` |
| showFAQSection                               |                    `2.80` |
| showSingleFAQ                                |                    `3.06` |
| showConversation (with no open Conversation) |                    `2.51` |
| showConversation (with an open Conversation) |                    `3.10` |
| showConversation (conversational mode)       |                    `3.92` |
| showInbox (Campaigns)                        |                    `0.60` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.1.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.27 MB to your application's release APK. This analysis was done with Android Plugin v3.1.1 and Gradle v4.4.
The 1.27 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.87 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7090 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7090` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `2.35` |
| showFAQSection                               |                    `1.24` |
| showSingleFAQ                                |                    `3.23` |
| showConversation (with no open Conversation) |                    `1.10` |
| showConversation (with an open Conversation) |                    `1.11` |
| showConversation (conversational mode)       |                    `1.55` |
| showInbox (Campaigns)                        |                    `0.51` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.0.1

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.71 MB to your application's release APK. This analysis was done with Android Plugin v3.1.1 and Gradle v4.4.
The 1.71 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.35 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7067 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7067` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `3.93` |
| showFAQSection                               |                    `2.39` |
| showSingleFAQ                                |                    `2.79` |
| showConversation (with no open Conversation) |                    `2.51` |
| showConversation (with an open Conversation) |                    `2.66` |
| showConversation (conversational mode)       |                    `3.10` |
| showInbox (Campaigns)                        |                    `0.60` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.0.1-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.22 MB to your application's release APK. This analysis was done with Android Plugin v3.1.1 and Gradle v4.4.
The 1.22 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.86 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7054 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7054` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `2.35` |
| showFAQSection                               |                    `0.84` |
| showSingleFAQ                                |                    `1.23` |
| showConversation (with no open Conversation) |                    `0.96` |
| showConversation (with an open Conversation) |                    `1.11` |
| showConversation (conversational mode)       |                    `1.55` |
| showInbox (Campaigns)                        |                    `0.51` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.0.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.74 MB to your application's release APK. This analysis was done with Android Plugin v3.0.0 and Gradle v4.1.
The 1.74 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.34 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7062 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7062` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `3.93` |
| showFAQSection                               |                    `2.39` |
| showSingleFAQ                                |                    `2.79` |
| showConversation (with no open Conversation) |                    `2.51` |
| showConversation (with an open Conversation) |                    `2.66` |
| showConversation (conversational mode)       |                    `3.10` |
| showInbox (Campaigns)                        |                    `0.60` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v7.0.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.27 MB to your application's release APK. This analysis was done with Android Plugin v3.0.0 and Gradle v4.1.
The 1.27 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.87 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 7049 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `7049` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.16` |
| showFAQs                                     |                    `2.35` |
| showFAQSection                               |                    `0.84` |
| showSingleFAQ                                |                    `1.23` |
| showConversation (with no open Conversation) |                    `0.96` |
| showConversation (with an open Conversation) |                    `1.11` |
| showConversation (conversational mode)       |                    `1.55` |
| showInbox (Campaigns)                        |                    `0.51` |
| showMessage (Campaigns)                      |                    `3.30` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.4.1

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.50 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.50 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.13 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6051 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6051` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `3.93` |
| showFAQSection                               |                    `2.37` |
| showSingleFAQ                                |                    `2.66` |
| showConversation (with no open Conversation) |                    `2.50` |
| showConversation (with an open Conversation) |                    `2.42` |
| showInbox (Campaigns)                        |                    `0.60` |
| showMessage (Campaigns)                      |                    `3.28` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.4.1-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.09 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.09 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.72 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6038 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6038` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `2.38` |
| showFAQSection                               |                    `0.82` |
| showSingleFAQ                                |                    `1.11` |
| showConversation (with no open Conversation) |                    `0.95` |
| showConversation (with an open Conversation) |                    `0.89` |
| showInbox (Campaigns)                        |                    `0.52` |
| showMessage (Campaigns)                      |                    `3.29` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.4.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.50 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.50 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.13 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6033 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6033` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.18` |
| showFAQs                                     |                    `3.89` |
| showFAQSection                               |                    `2.38` |
| showSingleFAQ                                |                    `2.74` |
| showConversation (with no open Conversation) |                    `2.49` |
| showConversation (with an open Conversation) |                    `2.41` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.4.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.09 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.09 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.72 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6020 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6020` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `2.33` |
| showFAQSection                               |                    `0.82` |
| showSingleFAQ                                |                    `1.18` |
| showConversation (with no open Conversation) |                    `0.93` |
| showConversation (with an open Conversation) |                    `0.84` |
| showInbox (Campaigns)                        |                    `0.49` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.3.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.50 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.50 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.13 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6023 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6023` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.14` |
| showFAQs                                     |                    `4.00` |
| showFAQSection                               |                    `2.48` |
| showSingleFAQ                                |                    `2.74` |
| showConversation (with no open Conversation) |                    `2.48` |
| showConversation (with an open Conversation) |                    `2.38` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.3.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.09 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.09 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.72 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6010 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6010` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `0.71` |
| showFAQSection                               |                    `2.55` |
| showSingleFAQ                                |                    `1.17` |
| showConversation (with no open Conversation) |                    `0.93` |
| showConversation (with an open Conversation) |                    `2.38` |
| showInbox (Campaigns)                        |                    `0.49` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.2.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.50 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.50 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.13 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6342 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6342` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `4.02` |
| showFAQSection                               |                    `4.11` |
| showSingleFAQ                                |                    `2.72` |
| showConversation (with no open Conversation) |                    `2.47` |
| showConversation (with an open Conversation) |                    `2.40` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.2.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.10 MB to your application's release APK. This analysis was done with Android Plugin v2.3.3 and Gradle v3.3.
The 1.10 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.73 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6329 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6329` |
| **com.android.support:design:26.0.2** (includes recyclerview) |       `5148` |
| **com.android.support:cardview-v7:26.0.2**                    |        `175` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `2.43` |
| showFAQSection                               |                    `2.54` |
| showSingleFAQ                                |                    `1.16` |
| showConversation (with no open Conversation) |                    `0.94` |
| showConversation (with an open Conversation) |                    `0.83` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.1.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.49 MB to your application's release APK. This analysis was done with Android Plugin v2.2.3 and Gradle v2.14.1.
The 1.49 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.12 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6262 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6262` |
| **com.android.support:design:25.3.1** (includes recyclerview) |       `4983` |
| **com.android.support:cardview-v7:25.3.1**                    |        `174` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `3.99` |
| showFAQSection                               |                    `4.11` |
| showSingleFAQ                                |                    `2.71` |
| showConversation (with no open Conversation) |                    `2.48` |
| showConversation (with an open Conversation) |                    `2.37` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.1.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.09 MB to your application's release APK. This analysis was done with Android Plugin v2.2.3 and Gradle v2.14.1.
The 1.09 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 0.72 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6249 methods to your dex:

| Package                                                       | Method Count |
| ------------------------------------------------------------- | -----------: |
| **com.helpshift**                                             |       `6249` |
| **com.android.support:design:25.3.1** (includes recyclerview) |       `4983` |
| **com.android.support:cardview-v7:25.3.1**                    |        `174` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.15` |
| showFAQs                                     |                    `2.41` |
| showFAQSection                               |                    `2.52` |
| showSingleFAQ                                |                    `1.15` |
| showConversation (with no open Conversation) |                    `0.93` |
| showConversation (with an open Conversation) |                    `0.67` |
| showInbox (Campaigns)                        |                    `0.59` |
| showMessage (Campaigns)                      |                    `3.38` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.2. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.0.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.7 MB to your application's APK. This analysis was done with Gradle v2.3.2 and Gradle wrapper v3.3.
The 1.7 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view).
If your app already contains appcompat, design, recycler-view and cardview, the Helpshift SDK will
add 1.3 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6250 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `6250` |
| **android.support:25.3.1**                   |      `19249` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9862` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6425` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1983` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `30` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.29` |
| showFAQs                                     |                    `3.80` |
| showFAQSection                               |                    `2.00` |
| showSingleFAQ                                |                    `1.74` |
| showConversation (with no open Conversation) |                    `2.30` |
| showConversation (with an open Conversation) |                    `2.37` |
| showInbox (Campaigns)                        |                    `0.84` |
| showMessage (Campaigns)                      |                    `3.94` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v6.0.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.2 MB to your application APK. This analysis was done with Gradle v2.3.2 and Gradle wrapper v3.3.
The 1.2 MB size includes total impact with dependencies on Android Support Libraries (design, recycler-view and card-view). If your app already contains appcompat, design,
recycler-view and cardview, Helpshift SDK will add 0.86 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 6237 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `6237` |
| **android.support:25.3.1**                   |      `19249` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9862` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6425` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1983` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `30` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.30` |
| showFAQs                                     |                    `1.20` |
| showFAQSection                               |                     `0.8` |
| showSingleFAQ                                |                    `0.60` |
| showConversation (with no open Conversation) |                    `1.00` |
| showConversation (with an open Conversation) |                    `0.70` |
| showInbox (Campaigns)                        |                     `0.6` |
| showMessage (Campaigns)                      |                     `1.5` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 5X running Android 7.1.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.9.1

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.5 MB to your application APK. This analysis
was done with Gradle v2.2.0 and Gradle wrapper v3.3. There was a size increase of
0.3 MB compared to previous versions due to the aforementioned Gradle configuration.
The 1.5 MB size includes total impact with dependencies on Android Support Libraries
(design, recycler-view and card-view). If your app already contains appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1.1 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4618 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4618` |
| **android.support:24.2.0**                   |      `19222` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9860` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6415` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1975` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `30` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.29` |
| showFAQs                                     |                    `3.90` |
| showFAQSection                               |                    `2.38` |
| showSingleFAQ                                |                    `2.56` |
| showConversation (with no open Conversation) |                    `2.29` |
| showConversation (with an open Conversation) |                    `1.23` |
| showInbox (Campaigns)                        |                     `0.8` |
| showMessage (Campaigns)                      |                    `3.94` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 7.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.9.1-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.3 MB to your application APK. This analysis
was done with Gradle v2.2.0 and Gradle wrapper v3.3. There was a size increase of
0.3 MB compared to previous versions due to the aforementioned Gradle configuration.
The 1.3 MB size includes total impact with dependencies on Android Support Libraries
(design, recycler-view and card-view). If your app already contains appcompat, design,
recycler-view and cardview, Helpshift SDK will add 0.9 MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4460 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4460` |
| **android.support:24.2.0**                   |      `19222` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9860` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6415` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1975` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `30` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.26` |
| showFAQs                                     |                    `3.88` |
| showFAQSection                               |                    `2.36` |
| showSingleFAQ                                |                    `2.56` |
| showConversation (with no open Conversation) |                    `2.25` |
| showConversation (with an open Conversation) |                    `1.74` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 7.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.9.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.2 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 0.9MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4592 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4592` |
| **android.support:24.2.0**                   |      `19222` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9860` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6415` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1975` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `30` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                     `1.2` |
| showFAQs                                     |                    `3.91` |
| showFAQSection                               |                    `2.42` |
| showSingleFAQ                                |                    `2.05` |
| showConversation (with no open Conversation) |                       `2` |
| showConversation (with an open Conversation) |                    `2.32` |
| showInbox (Campaigns)                        |                       `1` |
| showMessage (Campaigns)                      |                     `1.8` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 7. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.9.0-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 660KB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4572 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4572` |
| **android.support:24.2.0**                   |      `19220` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9859` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6415` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1975` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `29` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.49` |
| showFAQs                                     |                    `3.91` |
| showFAQSection                               |                    `3.24` |
| showSingleFAQ                                |                    `3.71` |
| showConversation (with no open Conversation) |                    `3.30` |
| showConversation (with an open Conversation) |                    `3.90` |
| showInbox (Campaigns)                        |                     `1.1` |
| showMessage (Campaigns)                      |                       `2` |

<Admonition type="info" title="Note">

Tested with a demo app on Samsung Galaxy S5 running Android 5.0. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.8.1

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.3 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4420 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4420` |
| **android.support:24.2.0**                   |      `17924` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9408` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6302` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1895` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `23` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.45` |
| showFAQs                                     |                    `3.15` |
| showFAQSection                               |                    `3.09` |
| showSingleFAQ                                |                    `3.91` |
| showConversation (with no open Conversation) |                    `3.03` |
| showConversation (with an open Conversation) |                     `3.6` |
| showInbox (Campaigns)                        |                     `1.1` |
| showMessage (Campaigns)                      |                       `2` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.8.1-english-only

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 0.9 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 0.6MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4416 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4416` |
| **android.support:24.2.0**                   |      `17924` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9408` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6302` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1895` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `23` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.45` |
| showFAQs                                     |                    `3.15` |
| showFAQSection                               |                    `3.09` |
| showSingleFAQ                                |                    `3.91` |
| showConversation (with no open Conversation) |                    `3.03` |
| showConversation (with an open Conversation) |                     `3.6` |
| showInbox (Campaigns)                        |                     `1.1` |
| showMessage (Campaigns)                      |                       `2` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.8.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.3 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4420 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4420` |
| **android.support:24.2.0**                   |      `17924` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9408` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6302` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1895` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `23` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `0.45` |
| showFAQs                                     |                    `3.15` |
| showFAQSection                               |                    `3.09` |
| showSingleFAQ                                |                    `3.91` |
| showConversation (with no open Conversation) |                    `3.03` |
| showConversation (with an open Conversation) |                     `3.6` |
| showInbox (Campaigns)                        |                     `1.1` |
| showMessage (Campaigns)                      |                       `2` |

<Admonition type="info" title="Note">

Tested with a demo app on OnePlus 3 running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.7.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.3 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4284 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4284` |
| **android.support:23.1.1**                   |      `18388` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `9665` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `6497` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1895` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `23` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `2.08` |
| showFAQs                                     |                    `7.12` |
| showFAQSection                               |                     `4.4` |
| showSingleFAQ                                |                    `4.73` |
| showConversation (with no open Conversation) |                    `3.79` |
| showConversation (with an open Conversation) |                    `6.84` |
| showInbox (Campaigns)                        |                    `1.70` |
| showMessage (Campaigns)                      |                    `2.84` |

<Admonition type="info" title="Note">

Tested with a demo app on Moto G4 Plus running Android 6.0.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.6.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.2 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4137 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4137` |
| **android.support:23.1.1**                   |      `16247` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `8775` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `5945` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1507` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `20` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `2.64` |
| showFAQs                                     |                    `8.00` |
| showFAQSection                               |                    `6.19` |
| showSingleFAQ                                |                    `6.53` |
| showConversation (with no open Conversation) |                    `4.86` |
| showConversation (with an open Conversation) |                    `5.63` |
| showInbox (Campaigns)                        |                    `1.63` |
| showMessage (Campaigns)                      |                    `3.94` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 4 running Android 5.1.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.5.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.2 MB to your application apk. This number
includes total impact with dependencies on Android Support Libraries (design,
recycler-view and card-view). If your app already has appcompat, design,
recycler-view and cardview, Helpshift SDK will add 1MB.

### Method count {#methods-count}

The Helpshift Android SDK will add 4093 methods to your dex:

| Package                                      | Method Count |
| -------------------------------------------- | -----------: |
| **com.helpshift**                            |       `4093` |
| **android.support:23.1.1**                   |      `16247` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v4         |       `8775` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.v7         |       `5945` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.design     |       `1507` |
| &nbsp;&nbsp;&nbsp;&nbsp;- support.annotation |         `20` |

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                           | Memory Usage (approx. MB) |
| -------------------------------------------- | ------------------------: |
| After adding Helpshift install call          |                    `2.00` |
| showFAQs                                     |                    `5.99` |
| showFAQSection                               |                    `7.15` |
| showSingleFAQ                                |                    `6.97` |
| showConversation (with no open Conversation) |                    `5.18` |
| showConversation (with an open Conversation) |                    `6.02` |
| showInbox (Campaigns)                        |                    `1.58` |
| showMessage (Campaigns)                      |                    `4.01` |

<Admonition type="info" title="Note">

Tested with a demo app on Nexus 4 running Android 5.1.1. Usage will
increase with number of FAQs and number of messages in a conversation.

</Admonition>

## v4.4.0

### SDK payload in your app {#payload-size}

The Helpshift Android SDK will add 1.06 MB to your application apk.

### Method count {#methods-count}

The Helpshift Android SDK will add 4071 method counts to your dex.

### Memory usage {#memory-usage}

Memory usage for the SDK screen launches.

| Support API launch                                   | Memory Usage (approx. MB) |
| ---------------------------------------------------- | ------------------------: |
| showConversation (with no Conversation)              |                    `1.32` |
| showConversation (with already running Conversation) |                    `0.63` |
| showFAQs                                             |                    ` 5.9` |
| showFAQSection                                       |                    `3.12` |
| showSingleFAQ                                        |                    ` 2.4` |
| showInbox (Campaigns)                                |                    `2.38` |
| showMessage (Campaigns)                              |                    `3.64` |

<Admonition type="info" title="Note">

Tested with a demo app. Usage will increase with number of FAQs and
number of messages in a conversation.

</Admonition>
