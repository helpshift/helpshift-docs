---
sidebar_position: 85
sidebar_class_name: feature-new
title: Proactive Engagement
description: "Proactive Engagement lets you reach out to your players by sending Push/In-App Messages on their mobile devices and engage in various ways including starting conversations."
---

import {
  Admonition,
  Intro,
  TabItem,
  Tabs,
  Image,
  Centered,
} from "@site/src/components/forDocs";

import AndroidiOSProactiveNotifBehaviourTable from "@site/docs/_partials/_androidAndiOSProactiveNotifBehaviourTable.mdx";
import InAppSample from "@site/docs/_partials/_proactiveAnd_inappSamples.mdx";

<Intro>

Proactive Engagement lets you reach out to your players by sending Push/In-App Messages on their mobile devices and engage in various ways including starting conversations.

</Intro>

<Admonition type="info" title="Note">

Proactive Engagement is available from SDK X 10.5.0 onwards.

All the public APIs in the SDK should be called after initializing the SDK via [Helpshift install API](/sdkx-react-native/getting-started#initializing)

Before starting with Proactive Engagement, please ensure you have integrated push notifications with Helpshift SDK. Please refer [Notifications Android](/sdkx-react-native/notifications-android) and [Notifications iOS](/sdkx-react-native/notifications-ios) page for integration guide.

</Admonition>

## Configuring Proactive Engagement Campaign {/* #configuring-proactive-engagement-notifications */}

To publish an Engagement Campaign follow these steps:

- Open the Helpshift Dashboard and navigate to **Settings > Workflows > Proactive Engagement**
- Create an Engagement campaign for the app you want with either **Push** or **In-App** message

<Image
  src="/static/books/common/proactive-admin-settings.png"
  width="full"
  alt="Helpshift admin dashboard settings for Proactive Engagement feature"
/>

- Select the desired action from available options, fill up the message content and add context as per your needs.
  - Intents/CIFs/tags/initial message etc configured here will be used when the end user starts a conversation by clicking on the message.

<Image
  src="/static/books/common/proactive-admin-compose.png"
  width="full"
  alt="Proactive Engagement campaign compose settings on Helpshift admin dashboard"
/>

- Configure the audience filter that you want to target. You can filter the audience based on:
  - Issue properties
  - User properties [using User Hub integration](/sdkx-react-native/identity-user-hub)

<Image
  src="/static/books/common/proactive-admin-audience.png"
  width="full"
  alt="Proactive Engagement campaign audience filter on Helpshift admin dashboard"
/>

- Schedule the engagement campaign. You will get a chance to review the full configuration before publishing the campaign.

<Image
  src="/static/books/common/proactive-admin-schedule.png"
  width="full"
  alt="Proactive Engagement campaign scheduling on Helpshift admin dashboard"
/>

### Push Message {/* #push-notifications */}

<Tabs groupId="sdk-platforms">
<TabItem value="android" label="Android">

<Image
  src="/static/books/sdkx_android/proactive-push-sdk-android.jpg"
  width="half"
  alt="Proactive Engagement push message on Android"
/>

- Push-type messages will be shown in the device notification tray.
- Use this type of message when you want to re-engage the user who has not been active or grab attention of users with some interesting information.
- With the various actions available on click of the message or the buttons in the message, you can redirect the user to any part of your application via deeplinks (need to be setup by the client application) or start a conversation via “Start Chat“ option or redirect them to an FAQ for quick help.
- Push messages will show up even if the app is not running in background.
- Rich text is supported in the content of the message. Make sure you provide compatible/supported HTML content used for setting up the message on Dashboard.
  - Due to system limitations, rich text in messages is not supported from Android 16 unless Android fixes it.

</TabItem>
<TabItem value="ios" label="iOS">

<Image
  src="/static/books/sdkx_ios/proactive-push-sdk-ios.png"
  width="half"
  alt="Proactive Engagement push message on iOS"
/>

- Push-type messages will be shown in the iOS Notification Center. Use this type of message when you want to re-engage the user who has not been active or grab attention of users with some interesting content.
- With the various actions available on click of the message, you can redirect the user to any part of your application via deeplinks (need to be setup by the client application) or start a conversation via “Start Chat“ option or redirect them to an FAQ for quick help.
- Push messages will show up even if the app is not running in the background.

<Admonition type="info" title="Note">

- Buttons in Push message is not supported for iOS
- Rich text is not supported in the content of the message

</Admonition>

<Admonition type="info" title="iOS: Notification Service Extension Requirement">

To support **Proactive Engagement push messages** with rich content on iOS
(for example, image-based messages),
your app must include a **Notification Service Extension (NSE)**.

If your app already has a Notification Service Extension, you can reuse it.
Otherwise, follow the setup instructions on the
[Notifications iOS](/sdkx-react-native/notifications-ios/#notification-service) page.

</Admonition>

</TabItem>
</Tabs>

### In-App Message {/* #in-app-notifications */}

<Tabs groupId="sdk-platforms">
<TabItem value="android" label="Android">

<InAppSample platform="android" />

- In-App messages are used to grab attention of the user who is already using your application, is on the application UI and you want to alert/engage them.
- If the user is not currently using the app, we show the in-app message when the application is opened next time. This way an in-app message is not lost even if user is not on the application screen.
- We currently support **Banner** type messages with click actions similar to Push messages.
- Note:
  - If the SDK receives multiple in-app messages when the app was in background then the SDK will show only the latest received in-app message and discard all the older ones.
  - By default, In-App messages expire in 2 days from the time of delivery to the device. If in-app received in background expires, then SDK will not show the expired in-app message.

</TabItem>
<TabItem value="ios" label="iOS">

<InAppSample platform="ios" />

- In-App messages are used to grab attention of the user who is already using your application, is on the application UI and you want to alert or engage with him in real time.
- If the user is not on the application UI then the message is shown in the notification tray of the device to engage the user.
- When the user clicks on the message we open the app and then present the actual configured in-app message. This increases the probability of user engagement and helps us achieve our goal better.
- We currently support Banner type messages with click actions similar to Push messages.
- If the SDK receives multiple in-app messages when the app was in background then the system will display all of them. On clicking the message, we will open the app and the SDK will show in-app message corresponding to the clicked message. Other messages remain as is in the notification center.
- By default, In-App messages expire in 2 days from the time of delivery to the device. If user clicks on a message that has already expired then on click no action will be taken.

</TabItem>
</Tabs>

## Behaviour in different configurations

<AndroidiOSProactiveNotifBehaviourTable />

## Pausing In-App Messages {/* #pausing-in-app-notifications */}

- This API will pause/unpause the showing of in-app messages to the end user when the application is in the foreground.
- You can use this API to pause in-app messages when you know that the user should not be disturbed. For example:
  - The user is on the app startup screen or the app is still loading.
  - The user is in active gameplay.
  - The user is on a payment or checkout screen.
- In-app messages received while they are paused are not lost; they are shown to the user as soon as the messages are unpaused.
- This API needs to be called in every app session because the SDK does not persist its value across sessions. The state of your app (e.g., whether the user is in gameplay) is best known to your application in real-time.
- By default, the SDK will always show in-app messages (`pauseDisplayOfInAppNotification` is effectively `false`) unless explicitly paused.
- **Values**:
  - `true`: Pauses in-app messages.
  - `false`: Resumes showing in-app messages (default behavior).

<div className="compact ">

| Key             | Details                                                 |
| --------------- | ------------------------------------------------------- |
| **API**         | `pauseDisplayOfInAppNotification(shouldPause: boolean)` |
| **Values**      | `true` (Pauses), `false` (Resumes)                      |
| **Min SDK**     | v10.5.0                                                 |
| **Call Timing** | After `install()`                                       |

</div>

Example:

```javascript
import { pauseDisplayOfInAppNotification } from "helpshift-plugin-sdkx-react-native";

// To temporarily pause in-app messages (e.g., during gameplay)
pauseDisplayOfInAppNotification(true);

// To resume showing the in-app messages
pauseDisplayOfInAppNotification(false);
```

## Setting defaults for message properties (Android) {/* #notification-defaults */}

If you need to set some defaults to be used in proactive messages on Android, you can use the `setAndroidProactivePushNotificationDefaults` API. We recommend calling this API right after the `install()` call.

The following configurations are available via this API:

- `notificationIconId`: Notification icon file name without extension, for the small icon to be used for proactive messages. If not configured, the app icon will be used by default.
- `largeNotificationIconId`: Large notification icon file name without extension. This will be used when an image is not configured in the message on the Helpshift Dashboard.
- `soundId`: Sound resource name without extension, for the sound to be used for proactive messages. If not provided, the system default sound will be used. Note that this sound will be associated with the default message channel that the SDK creates for Proactive Engagement messages.
- `proactiveSupportChannelId`: Custom channel ID for Proactive support cases (messages leading to Helpcenter or Conversations).
- `proactiveEngageChannelId`: Custom channel ID for Proactive Engage cases (messages leading to user engagement).

<div className="compact ">

| Key             | Details                                                       |
| --------------- | ------------------------------------------------------------- |
| **API**         | `setAndroidProactivePushNotificationDefaults(config: object)` |
| **Platform**    | Android only                                                  |
| **Min SDK**     | v10.5.0                                                       |
| **Call Timing** | Right after `install()`                                       |

</div>

Example:

```javascript
import {
  install,
  setAndroidProactivePushNotificationDefaults,
} from "helpshift-plugin-sdkx-react-native";

// ... install call ...

const proactiveDefaults = {
  notificationIconId: "ic_notification",
  largeNotificationIconId: "ic_large_notification",
  soundId: "notification_sound",
  proactiveSupportChannelId: "my_support_channel",
  proactiveEngageChannelId: "my_engage_channel",
};

setAndroidProactivePushNotificationDefaults(proactiveDefaults);
```

### Custom message channels for Proactive Engagement messages {/* #custom-channels */}

If you want to use your own message channels (created by your application beforehand) for Proactive Engagement messages, you can provide the channel IDs via the API above.

If the values are not set, the SDK creates default message channels automatically:

- **Proactive Support**: For messages leading to Helpcenter or Conversations. Default name: "Proactive Support".
- **Proactive Engage**: For messages leading to user engagement. Default name: "Proactive Engage".

For more details on these configuration options and custom message channels, please refer to the [Android Notification Defaults](/sdkx-react-native/notifications-android/#notification-defaults) section.

## Passing configuration specific to the current user {/* #user-specific-config */}

- You may want to add configuration specific to the current user in your app when they interact with proactive messages.
- This configuration is used when no configuration is provided from the Helpshift Dashboard during message creation. The configuration data format is the same as what you would use in other APIs like `showConversation` or `showFAQs`.
- The configuration provided here will also be used for the next issue filed in the same session. For example, if a user starts a conversation via a proactive message and that conversation is resolved, any subsequent conversation started in the same session will use this fallback configuration.
- **Important**: You must call this API after the `install` call and before the user interacts with (clicks) the message.

To set this configuration, use the `setProactiveConfig` API.

```javascript
import { setProactiveConfig } from "helpshift-plugin-sdkx-react-native";

const proactiveConfig = {
  initialUserMessage: "Hi there!",
  fullPrivacy: true,
  tags: ["vip", "payment", "renewal"],
  cifs: {
    is_pro: { type: "boolean", value: true },
    stock_level: { type: "number", value: "1505" },
  },
};

setProactiveConfig(proactiveConfig);
```
