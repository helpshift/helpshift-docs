---
sidebar_position: 120
title: インパクト分析
description: "SDKのリリースごとに、このページではSDKがアプリケーションのビルドと実行時間のさまざまな側面に与える影響を追跡します。"
---

import {
  Admonition,
  CodeBlock,
  Tabs,
  TabItem,
  LatestSdkVersion,
  Centered,
  Image,
  Intro,
  SideBySide,
  DownloadButton,
  Steps,
  Step,
} from "@site/src/components/forDocs";

# インパクト分析 {#performance}

<Intro>

SDKのリリースごとに、このページではSDKがアプリケーションのビルドと実行時間のさまざまな側面に与える影響を追跡します。

</Intro>

<Admonition type="info" title="注意">

**アプリの起動時間**

Helpshiftの初期化はすべてバックグラウンドのスレッドで実行されるため、Helpshift SDKがアプリの起動時間に影響を与えることはありません。

</Admonition>

# バージョン別のレポート {#version-impact}

Helpshift SDKの各バージョンのインパクト分析レポートについては、以下でご確認いただけます。

## 10.5.0

<Admonition type="info" title="注意">

macOS 15.7.2、Xcode 26.1.1、iOS 26.1（iPhone 15 Plus）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが4.6MB、シミュレーターが5.6MBです。合計サイズは10.3MBです。

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 1.00             | 0.93                |
| x86_64       | NA               | 1.01                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`603`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>5.2</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。     |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>4.1</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>2.8</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>4.3</code>MBが追加されます。                                                         |

## 10.4.0

<Admonition type="info" title="注意">

macOS 14.6.1、Xcode 16.0.0、iOS 18.1.1（iPhone 15）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが7.2MB、シミュレーターが8.1MBです。合計サイズは15.3MBです。

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 0.85             | 0.83                |
| x86_64       | NA               | 0.77                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`647`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>4.1</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。     |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>3.6</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>3.5</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>3.3</code>MBが追加されます。                                                         |

## 10.3.0

<Admonition type="info" title="注意">

macOS 13.5.1、Xcode 15.0.0、iOS 17.0.3（iPhone 15 Plus）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが1.2MB、シミュレーターが2MBです。合計サイズは3.2MBです。

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 0.85             | 0.83                |
| x86_64       | NA               | 0.77                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`366`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>3.7</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。     |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>2.8</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>4.1</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>2.3</code>MBが追加されます。                                                         |

## 10.2.0

<Admonition type="info" title="注意">

macOS 11.4、Xcode 12.5.1、iOS 15.1（iPhone XR）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが7.4MB、シミュレーターが5.3MBです。合計サイズは12.7MBです。

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 3.45             | 3.55                |
| armv7        | 3.37             | NA                  |
| i386         | NA               | 0.61                |
| x86_64       | NA               | 0.67                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`268`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>3.5</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。 |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>3.2</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>3.4</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>3.4</code>MBが追加されます。                                                         |

## 10.1.0

<Admonition type="info" title="注意">

macOS 11.4、Xcode 12.5.1、iOS 15.1（iPhone XR）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが6.6MB、シミュレーターが4.7MBです。合計サイズは11.3MBです。

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 3.09             | 3.17                |
| armv7        | 2.98             | NA                  |
| i386         | NA               | 5.32                |
| x86_64       | NA               | 5.89                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`240`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>4.6</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。 |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>3.9</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>4.1</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>4.3</code>MBが追加されます。                                                         |

## 10.0.0

<Admonition type="info" title="注意">

OS X 10.15.7、Xcode 12.0、iOS 14.0（iPhone X）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

xcframeworkが占有するディスク容量です。

xcframeworkのサイズは、デバイスが5.9MB、シミュレーターが4.2MBです

内訳は以下の通りです。

| アーキテクチャ | デバイスのサイズ（MB） | シミュレーターのサイズ（MB） |
| ------------ | ---------------- | ------------------- |
| arm64        | 2.87             | 2.92                |
| armv7        | 2.81             | NA                  |
| i386         | NA               | 5.11                |
| x86_64       | NA               | 5.53                |

iOSアプリが`HelpshiftX.xcframework`と統合された場合、IPAファイルサイズの増加は約`200`KBです。

### RAMの使用量

|                                                                   |                                                                                   |
| ----------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）**      | <code>4.8</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。 |
| **HelpshiftのFAQ画面（<code>showFAQs:</code>）**              | <code>4.4</code>MBが追加されます。                                                         |
| **HelpshiftのFAQセクション画面（<code>showFAQSection:</code>）** | <code>4.3</code>MBが追加されます。                                                         |
| **Helpshiftの単一のFAQ画面（<code>showFAQ:</code>）**         | <code>4.1</code>MBが追加されます。                                                         |

## 1.0.0-beta

<Admonition type="info" title="注意">

OS X 10.15.6、Xcode 10.2、iOS 12.4.5（iPhone 6 Plus）の環境で実施しています。メモリの使用量は、会話内のメッセージ数に応じて増加します。

</Admonition>

### ディスク容量

Dynamic Frameworkが占有するディスク容量です。

フレームワークの合計サイズは`1.7 MB`です。

内訳は以下の通りです。

| アーキテクチャ | サイズ（MB） |
| ------------ | --------- |
| arm64        | 0.421     |
| armv7        | 0.373     |
| i386         | 0.368     |
| x86_64       | 0.372     |

iOSアプリがHelpshiftXフレームワークと統合された場合、IPAファイルサイズの増加はフレームワークのビットコード版と非ビットコード版の両方について約`140 KB`KBです。

### RAMの使用量

メモリの使用量（デバッグモードでは、値はおおよそのものです。）

|                                                              |                                                                                   |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------- |
| **Helpshiftのチャット画面（<code>showConversation:</code>）** | <code>4.5</code>MBが追加されます（チャット画面上のメッセージの数に依存します）。 |
